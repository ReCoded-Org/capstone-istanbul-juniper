import React from "react";
import { Row, Col } from "antd";

const ListOfImages = () => {
  // The cols in the return state are going to contain a list of clickable images
  // each image when clicked will show the puzzle of that specific image
  return (
    <Row>
      <Col>something</Col>
      <Col>something</Col>
      <Col>something</Col>
      <Col>something</Col>
    </Row>
  );
};

export default ListOfImages;
